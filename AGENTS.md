# Agent Instructions

## Review loop

Before finalizing code changes, run at least three independent code-review agents
using models other than the model currently driving the session. Address all
valid feedback, then re-run the same multi-model review loop until every review
returns no actionable comments.
